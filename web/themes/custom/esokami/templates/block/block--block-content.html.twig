{#
/**
 * @file
 * Minimal block template for block_content (basic blocks).
 *
 * Strips the default Bootstrap wrapper markup so that homepage sections
 * (hero, section-alt, section-dark, etc.) render full-width with their
 * own classes intact.
 */
#}
{{ title_prefix }}
{{ title_suffix }}
{{ content }}
